(function($) {

    $(function(){
        $('.l-header__menu-icon, .l-sidebar__menu-icon, .c-filter').on('click', function () {
            $('.l-header__menu-icon, .l-sidebar, .l-sidebar__menu-icon, .c-filter').toggleClass('show');
        });
        })

     var myelement = $( '.elem' );
})( jQuery );
